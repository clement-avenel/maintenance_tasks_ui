#!/bin/bash -e

# Ensure DB and tables exist when starting the server (ephemeral disk: release_command runs on a different instance)
if echo " $* " | grep -q " rails .* server "; then
  bundle exec rails db:prepare
fi

exec "$@"

<p>
  Ran for <%= time_running_in_words run %> until paused,
  <% if run.tick_count.present? && run.tick_total.present? && (time_to_completion = run.time_to_completion) %>
    <%= distance_of_time_in_words(time_to_completion) %> remaining.
  <% else %>
    processed <%= pluralize run.tick_count.to_i, "item" %> so far.
  <% end %>
</p>
